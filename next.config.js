/** @type {import('next').NextConfig} */
const nextConfig = {
    // experimental: {
    //     serverActions: true,
    // },
    images: {
        domains: ['pyraigecudoitbrpnjfo.supabase.co'],
    },
};

module.exports = nextConfig
